<ul id="lists">
  <% @lists.each do |list| %>
    <li class="<%= list[:all_completed] ? 'complete' : '' %>">
      <a href=<%= "lists/#{list[:id]}" %>>
        <h2><%= list[:name] %></h2>
        <p><%= list[:todos_completed_count] %> / <%= list[:todos_count] %></p>
      </a>
    </li>
  <% end %>
</ul>

<% content_for :header_links do %>
  <a class="add" href="/lists/new">New List</a>
<% end %>